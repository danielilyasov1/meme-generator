"use strict"
function onSetTextLine(txt) {
  setTextLine(txt)
//   renderMeme()
}

function renderMeme() {
  ///////////////////////////////////////////////////////////
}

function onChangeFontSize(diff) {
  changeFontSize(diff)

  renderMeme()
}
